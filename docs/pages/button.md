---
title: Button
slug: button
group: UI
order: 0
description: Button styles with shadcn-like variants and sizes.
status: beta
tags: [button, ui, styles]
---

A styled button with shadcn-like variants.

:::demo id=button-basic title="Variants and sizes"
Shows `default`, `secondary`, `outline`, `ghost`, `link`, `destructive` and `sm/default/lg/icon` sizes.
:::

:::note
This is a styling + tiny helper (`Button`) that returns an `HTMLButtonElement` with the right classes. You can also apply the classes directly.
:::

## Props

:::props name=Button
:::

## Code

:::code file=src/docs/examples/button_basic.dart region=snippet lang=dart
:::
